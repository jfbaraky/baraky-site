{
  "name": "baraky-site",
  "version": "1.0.0",
  "description": "Personal Baraky site",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next build && next start -p $PORT",
    "export": "next export",
    "deploy": "next build && next export && firebase deploy"
  },
  "author": "Baraky",
  "license": "ISC",
  "dependencies": {
    "next": "^9.3.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "styled-components": "^4.2.0"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.0"
  }
}
